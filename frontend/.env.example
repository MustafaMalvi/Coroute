
# Mapbox GL access token used by the live ride-tracking map (src/components/mapbox.jsx).
# Get one at https://account.mapbox.com/access-tokens/ and restrict it to your domain.
VITE_MAPBOX_TOKEN=pk.your_mapbox_public_token
VITE_BACKEND_URL=http://localhost:5000